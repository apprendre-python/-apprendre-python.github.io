import CodeEditor from '../../src/components/CodeEditor'
import Details from '@theme/Details';

# Variables

```py Exemple
a = 3
b = a
a = 4
```	

```python
def derive(f, x, h):
    return (f(x + h) - f(x))/h

def g(x):
    return x**2

print(derive(g, 2, 0.01))
```
